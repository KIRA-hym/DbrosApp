import { GoogleGenerativeAI } from '@google/generative-ai'; const genAI = new GoogleGenerativeAI('DUMMY_KEY'); const model = genAI.getGenerativeModel({ model: 'non-existent-model' }); model.generateContent('hi').catch(e => console.log(e.message));
