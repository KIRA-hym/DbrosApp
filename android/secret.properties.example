# Google Cloud Console에서 발급한 Maps SDK for Android 키 (한 줄, 따옴표 없음)
# 사용법: 이 파일을 secret.properties 로 복사한 뒤 값만 채우거나,
#        android/local.properties 맨 아래에 MAPS_API_KEY=키값 추가 (secret.properties 가 우선)
#
# 키 제한: 패키지 com.example.dbros_app, SHA-1 은 gradlew :app:signingReport 로 확인
MAPS_API_KEY=
