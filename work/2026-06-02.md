# 2026-06-02 작업 내역

## 1. 빌드 및 릴리즈 프로세스 안정화
- Gradle Heap OOM 이슈 해결 (-Xmx4096m 설정)
-  uild_release.ps1 스크립트 보완 (shorebird 프롬프트 자동화)
- v1.0.08.02 전체 빌드 및 Firebase 랜딩 페이지 배포 성공

## 2. 로그인 화면 UI 변경
- 서브타이틀 ('대리운전 기사님들을 위한 완벽한 보조앱') 제거 완료

## 3. 설정 화면 개선
- 공지사항/알림 버튼 반으로 축소 및 왼쪽에 배치
- 업데이트 안내 페이지 (https://dbros-install.web.app/) 접속 버튼 우측에 추가
- 푸시 발송 하단에 관리자 회원 관리 기능 연결 버튼 신설 (AdminUserListPage 재사용)

## 4. Shorebird 업데이트 팝업 및 로직 개선 (UX)
- `shorebird_update_dialog.dart`: [재시작] 버튼 동작을 `exit(0)`(프로세스 강제 종료)로 변경하여 콜드 스타트로 패치가 확실히 적용되게 개선.
- `shorebird_update_dialog.dart`: [나중에 하기] 버튼 추가하여 강제 재시작 방지.
- `shorebird_update_service.dart`: 패치 보류 상태 저장 로직(`postponeUpdate`) 및 수동 업데이트 확인용 반환형(`Future<bool>`) 추가.
- `settings_page.dart`: 패치를 미룬 유저에게 설정화면 [업데이트] 버튼에 빨간 점(알림뱃지) 표시. 버튼 클릭 시 [패치적용]과 [APK다운] 선택 팝업 제공. 뱃지가 없을 땐 즉시 수동 업데이트 검사 수행.
- `main.dart`: 백그라운드 복귀(`AppLifecycleState.resumed`) 시 1시간 쿨타임으로 자동 패치 스캔 로직 추가.
