# 2026-06-04 작업 일지

## 1. 🔍 OCR 식별 로직 (휴리스틱) 강화
- **파일**: `lib/services/call_card_ocr_parse_service.dart`
- **내용**: `_detectProgram` 메서드에서 프로그램 오인식을 방지하기 위해 식별 기준 강화.
  - **로지**: `배차`, `갱신`, `닫기` 3개의 키워드가 **모두** 포함된 경우에만 로지로 인식.
  - **콜마너**: `출도`, `길안내` 2개의 키워드가 **모두** 포함된 경우에만 콜마너로 인식.

## 2. 🛡️ 단건/다건 갤러리 등록 시 빈 일지 저장 차단
- **파일**: `lib/screens/single_call_card_page.dart`, `lib/screens/multi_call_card_page.dart`
- **내용**: 
  - 기존에는 자동 감지(`screenshot_auto`)에서만 사용되던 유효성 검사 `CallCardOcrParseService.isValidForAutoSave` 로직을 갤러리를 통한 수동 등록(단건/다건) 화면에도 동일하게 적용.
  - 요금 미인식, 출발지/도착지 누락, 프로그램 미식별 등 유효하지 않은 쓰레기 데이터는 DB에 저장되지 않고 '등록 실패' 건수로 처리되도록 로직 수정.
  - 다건/단건 저장 시 자체 `insert` 쿼리를 사용하던 부분을 `CallCardOcrParseService.saveLogToDatabase` 통합 메서드를 호출하도록 리팩토링하여 중복 체크 로직을 일원화함.

## 3. ⏱️ 캡처 이미지 중복 저장 쿼리 조건 변경
- **파일**: `lib/services/call_card_ocr_parse_service.dart`
- **내용**: `saveLogToDatabase` 메서드의 중복 판별 쿼리 보완.
  - 기존: `created_at > 2시간 전` (2시간 후면 동일 이미지를 다시 올려도 중복으로 걸러지지 않음)
  - **변경**: `drive_date = ?` 조건으로 변경하여, 동일한 운행일자(drive_date), 요금, 출발지, 도착지, 프로그램 정보를 가진 일지는 시간에 구애받지 않고 **중복 저장되지 않도록 완벽하게 차단**.

## 4. ⌨️ 수기 입력 화면 키패드 깜빡임(사라짐) 버그 해결
- **파일**: `lib/screens/write_log_page.dart`
- **내용**: 특정 단말기에서 입력칸 터치 시 안드로이드 키보드가 올라오다가 번쩍하고 사라져 입력이 불가하던 심각한 버그 수정.
  - 원인: 텍스트 필드 포커스 시점으로부터 300ms 후에 강제로 화면 중심부로 스크롤을 튀게 만드는 `_setupFocusScroll` 안의 `Scrollable.ensureVisible` 로직이 안드로이드 순정 키보드 애니메이션과 충돌함.
  - 해결: 해당 강제 스크롤 로직(`_setupFocusScroll`) 및 리스너 등록부를 완전히 제거하여, Flutter가 제공하는 순정 뷰포트 조절 기능(`resizeToAvoidBottomInset`)만 부드럽게 작동하도록 원상복구함.

## 5. 🚀 배포 사항
- 위 사항 적용 후 `shorebird patch android` 명령어를 통해 **1.0.08+100803 릴리스 버전에 대한 원격 패치(Patch 1) 완료**.
- 앱을 재시작하는 모든 사용자 기기에 스토어 심사 없이 즉시 패치 내용 적용됨.

## 6. ✨ 스플래시 화면 아이콘 고해상도 교체
- **파일**: `pubspec.yaml`
- **내용**: 
  - 스플래시 화면에서 로고 이미지가 확대되어 해상도가 깨지는 현상을 방지하기 위해, `flutter_native_splash` 설정에서 기본 이미지와 안드로이드 12 전용 이미지를 모두 고해상도 파일(`assets/icon2.png`)로 변경함.
  - `dart run flutter_native_splash:create` 명령어로 네이티브 스플래시 에셋 재생성 완료.

## 7. 🎨 UI/UX 디자인 일관성 개선

### 7-1. 콜카드 단건등록 AppBar 타이틀 색상 수정
- **파일**: `lib/screens/single_call_card_page.dart` (L182~191)
- **내용**: AppBar 타이틀 색상이 흰색(`Colors.white`)으로 다른 화면(골드 `#FFC700`)과 불일치.
  - 직접 `TextStyle` 하드코딩 방식 → `Theme.of(context).textTheme.titleLarge?.copyWith(color: Color(0xFFFFC700))`으로 변경.
  - 다른 화면(`stats_page`, `settings_page` 등)과 동일한 ThemeData 기반 방식으로 통일.

### 7-2. AppColors 색상 상수 파일 생성
- **파일**: `lib/constants/app_colors.dart` (신규 생성)
- **내용**: 앱 전체에 산재된 하드코딩 색상을 중앙화하기 위한 상수 클래스 생성.
  - `primary` (`#FFC700`), `background` (`#121418`), `surface` (`#1F222A`), `surfaceDeep` (`#16181D`), `divider` (`#2C2F38`), `textSecondary` (`#6E717C`) 정의.
  - 기능 색상: `income` (수입, `Colors.lightBlueAccent`), `expense` (지출, `#FF5252`), `net` (순익, `#FFC700`), `exportAction`, `importAction` 정의.
  - 향후 색상 수정 시 이 파일 한 곳만 변경하면 전체 화면에 반영됨.

### 7-3. write_log_page `_buildInputGroup` BorderedSection 재사용
- **파일**: `lib/screens/write_log_page.dart` (L1647~1653)
- **내용**: `_buildInputGroup` 메서드가 `BorderedSection.decoration()`과 완전히 동일한 내용의 `BoxDecoration`을 직접 정의하고 있던 중복 코드 제거.
  - `BoxDecoration(color: 0xFF1F222A, borderRadius: 12, border: white10)` → `BorderedSection.decoration()` 1줄로 교체.
  - `bordered_section.dart` import 추가.

### 7-4. 홈화면 "운행 일지 관리" 버튼 오너 권한 체크
- **파일**: `lib/screens/home_page.dart` (L254~285)
- **내용**: 기존에는 오너가 아닌 사용자도 버튼을 누르면 화면이 이동하고 해당 화면에서 "권한 없음"을 표시하는 방식이었음.
  - 변경: `SettingsService.isOwnerMode`를 버튼 탭 시점에 체크하여, 오너가 아닐 경우 화면 이동 없이 SnackBar로 안내 메시지만 표시하고 `return`.
  - 추가: 오너가 아닐 때는 버튼 텍스트 색상도 골드 대신 비활성 회색(`#6E717C`)으로 표시하여 시각적으로도 비활성 상태임을 알림.
  - `settings_service.dart` import 추가.

### 7-5. 콜카드 다중등록 AppBar 타이틀 색상 수정
- **파일**: `lib/screens/multi_call_card_page.dart` (L276~284)
- **내용**: `single_call_card_page`와 동일한 패턴으로, 직접 `TextStyle` 하드코딩 방식 → `Theme.of(context).textTheme.titleLarge?.copyWith(color: Color(0xFFFFC700))`으로 변경.

### 7-6. `stats_page` 수입 표시 색상 통일
- **파일**: `lib/screens/stats_page.dart`
- **내용**: 하드코딩된 `Colors.green` 대신 공식 색상인 `AppColors.income`을 사용하여 수입/매출 표시 색상을 앱 전반적으로 통일.

### 7-7. `AppEmptyState` 공통 위젯 추출
- **새 파일**: `lib/widgets/app_empty_state.dart`
- **파일 수정**: `lib/screens/single_call_card_page.dart`, `lib/screens/multi_call_card_page.dart`
- **내용**: 두 화면에 동일한 로직으로 복제되어 있던 빈 화면(`_buildEmptyState`) 코드를 `AppEmptyState` 공통 위젯으로 추출하여 코드 중복을 제거하고 유지보수성을 개선함.

### 7-8. 사용하지 않는 코드 및 임포트 정리 (Lint 오류 수정)
- **파일**: `lib/screens/home_page.dart`, `lib/screens/single_call_card_page.dart`, `lib/screens/multi_call_card_page.dart`, `lib/screens/stats_page.dart`
- **내용**: `flutter analyze`를 통해 발견된 미사용 패키지 임포트, 더 이상 참조하지 않는 로컬 변수, 그리고 사용되지 않는 위젯 렌더링 메소드(`_buildExpandedStatsMetricsColumn` 등)를 완전히 제거하여 소스 코드를 깔끔하게 정리함.
