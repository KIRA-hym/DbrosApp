# 2026년 07월 01일 작업 내역

## 진행 완료 내역
1. **앱 가이드화면(코치마크 vs 온보딩) 기능 테스트용 데모 구현 및 배포**
   - 코치마크와 온보딩 화면의 차이를 체험할 수 있는 웹용 데모 페이지(`onboarding_demo.html`) 구현
   - DbrosLanding Firebase Hosting을 통해 배포 완료
2. **앱 메인 화면(홈) 코치마크 가이드 구현**
   - `tutorial_coach_mark` 패키지 설치
   - `GuideProvider`를 통한 상태 관리 및 `home_page.dart` 연동 완료
   - 설정 화면에 "앱 이용 가이드" 메뉴 생성, 사용자가 필요 시 코치마크 실행 가능 (On-Demand)
3. **목록 화면 & 일일 상세 화면 코치마크 구현**
   - 홈/목록/상세에서 공통으로 쓰는 `GuideContentWidget` 생성 (다크 테마 둥근 상자, '다음>' 버튼)
   - `log_list_page.dart`의 `_LogListPageState`에 목록 화면 가이드 4종 타겟(월간 이동, 월간 공유, 상세 진입, 요약) 연결 완료
   - `log_list_page.dart`의 `_DailyLogListPageState`에 상세 화면 가이드 4종 타겟(날짜 이동, 지도 맵, 일지 삭제, 일일 공유) 연결 완료
   - `settings_page.dart` 내 가이드 실행 바텀시트 메뉴에 목록, 상세 화면 가이드 시작 버튼 연결 및 화면 이동 처리 완료
   - Web Server 환경(port 3000)에서 실행 및 동작 검증 준비 완료
4. **앱 버전 1.0.13+101305 배포 진행 완료**
   - `dbros-release` 스킬의 지침에 따라 릴리즈 빌드 완료 (`shorebird release android --artifact apk`)
   - Firebase Hosting (`dbros-install.web.app`) 에 다운로드 가능한 APK 업데이트 및 배포 완료
   - DbrosApp 및 DbrosLanding Git 커밋 및 푸시 완료
