# Dbros Admin 프로젝트 AI 작업 규칙 (Rules)

이 프로젝트(`DbrosAdmin`)에서 AI(Antigravity)가 작업을 수행할 때 반드시 지켜야 하는 필수 규칙입니다. 이 규칙은 앱(Flutter) 프로젝트의 작업 방식과 동일한 흐름을 유지하되, 배포 방식만 본 웹 프로젝트에 맞게 적용됩니다.

## 1. 작업 프로세스 (Plan -> Approve -> Execute)
- **요청 분석 및 계획**: 사용자가 새로운 기능이나 수정을 요청하면, **코드를 바로 수정하지 않습니다.**
- **계획서 작성**: 요청을 어떻게 이해했는지, 어떤 파일을 어떻게 수정할 것인지 구체적인 계획을 정리하여 사용자에게 먼저 제시합니다. (`implementation_plan.md` 활용)
- **진행 승인**: 사용자가 "진행해", "승인" 등의 피드백을 주면 비로소 실제 코드 수정 및 작업을 시작합니다.

## 2. 작업 관리
- 작업이 시작되면 `task.md`를 통해 체크리스트를 만들어 진행 상황을 관리합니다.
- 기존 코드의 의도와 주석을 보존하며, 꼭 필요한 부분만 수정하여 부작용(Side-effect)을 최소화합니다.

## 3. 커밋 및 푸시 (Commit & Push)
- 작업이 모두 완료되고 테스트에 이상이 없다면, 변경 사항을 Git에 반영합니다.
- `git add .` -> `git commit -m "feat: 작업 내용 상세 설명"` -> `git push`
- 커밋 메시지는 어떤 파일이 왜 변경되었는지 명확하게 알 수 있도록 상세히 적습니다.

## 4. 배포 (Deploy) - *Admin 프로젝트 전용*
- 커밋과 푸시가 완료되면, 파이어베이스 호스팅에 즉시 배포하여 라이브 환경에 반영합니다.
- 명령어: `npm run build && npx firebase-tools deploy --only hosting --non-interactive`

## 5. 완료 보고
- 위의 모든 절차(계획 -> 코드 수정 -> 커밋/푸시 -> 배포)가 끝나면 사용자에게 최종 완료를 보고합니다.
